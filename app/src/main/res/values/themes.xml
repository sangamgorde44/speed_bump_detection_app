<!-- res/values/styles.xml -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.SpeedBumpDetectionApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here -->
        <item name="colorPrimary">@color/main_color_1</item>

    </style>

    <style name="myThemeNoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
        <item name="colorPrimary">@color/main_color_1</item>
        <item name="colorAccent">@color/white</item>

    </style>

    <style name="Theme.SpeedBumpDetectionApp" parent="Base.Theme.SpeedBumpDetectionApp" />


    <!-- Custom theme for toolbar to handle overflow icon color -->
    <style name="CustomToolbarTheme" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:tint">@color/main_color_1</item>
        <item name="titleTextColor">@android:color/white</item>
        <item name="android:background">#D8FFFFFF</item>
    </style>


    <!-- Custom theme for toolbar to handle overflow icon color -->
    <style name="CustomBottomBarTheme" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:tint">@color/white</item>
        <item name="android:subtitleTextColor">@color/white</item>
        <item name="titleTextColor">@android:color/white</item>
        <item name="android:background">@color/main_color_1</item>

    </style>





</resources>
